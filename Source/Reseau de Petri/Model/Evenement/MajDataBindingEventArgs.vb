Public Class MajDataBindingEventArgs
    Inherits EventArgs 'Elle hérite donc de la classe System.EventArgs qui est la classe de base des classes contenant des données d'événement.
    Public Sub New() 'mise en place du constructeur lié à cette classe, qui permettra d'initialiser l'instanciation des objets liés à cette classe.
    End Sub
End Class